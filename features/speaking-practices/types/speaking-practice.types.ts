export interface SpeakingPractice {
  id: string;
  protectedAreaId: string;
  title: string;
  instructions: string;
  providerId: string;
  model: string;
  prompt: string;
  isActive: boolean;
  createdAt: string;
}
